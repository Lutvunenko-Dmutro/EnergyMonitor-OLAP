# 📚 EnergyMonitor-OLAP Documentation (gh-pages)

This branch is **auto-generated** by MkDocs (via `mkdocs gh-deploy`). It contains the compiled HTML files for the **ProperDocs** static documentation site.

👉 **[View the Live Documentation Here](https://lutvunenko-dmutro.github.io/EnergyMonitor-OLAP/)**

> [!WARNING]
> Do not commit changes directly to this branch! Any manual changes here will be overwritten the next time the documentation is built. 
> To update the documentation, switch to the `main` branch, edit the Markdown files in the `docs/` folder, and re-run `mkdocs gh-deploy`.

## Branch Structure
- `index.html` - The entry point of the site.
- `assets/` - Static assets (CSS, JS, fonts).
- `system/` - Compiled HTML passports for the architectural modules.
